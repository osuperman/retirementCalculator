OPENAI_API_KEY=sk-your-openai-api-key
OPENAI_MODEL=gpt-5.4-mini
LLM_PROVIDER=openai

# For static hosting, point the browser at a separate backend endpoint.
# Leave blank for local Vite dev, which serves /api/chat through vite.config.js.
# VITE_CHAT_API_URL=https://your-backend.example.com/api/chat
